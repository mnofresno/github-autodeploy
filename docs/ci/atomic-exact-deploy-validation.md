# Atomic exact deployment validation

Temporary marker used to execute the full CI suite against `master` after making fetch, FETCH_HEAD verification, reset, and HEAD verification a single command. This file must not be merged.
