# Exact SHA named-ref validation

Temporary marker used to execute the full CI suite against `master` after fetching the requested deployment SHA into `refs/git-autodeploy/deploy-target`. This file must not be merged.
