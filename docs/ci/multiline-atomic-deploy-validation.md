# Multiline atomic exact deployment validation

Temporary marker used to execute the full CI suite against `master` after routing the atomic deployment command through `bash -c`. This file must not be merged.
